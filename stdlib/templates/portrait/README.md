Description: Character portrait sprite template
Category: character

## Params

- `expression` (string, default: "happy") — Facial expression
- `style` (string, default: "cel") — Art style: cel, pixel, flat
- `bg` (string, default: "transparent") — Background color
- `name` (string, default: "character") — Output filename stem

## Output

128x256 PNG sprite with center-bottom anchor.