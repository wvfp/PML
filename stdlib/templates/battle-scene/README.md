Description: Horizontal battle scene with hero and enemy
Category: scene

## Params

- `hero-expression` (string, default: "happy") — Hero's facial expression
- `enemy-type` (string, default: "goblin") — Enemy character type
- `bg-type` (string, default: "plains") — Background environment
- `width` (number, default: 400) — Scene width
- `height` (number, default: 200) — Scene height

## Output

400x200 PNG scene with layered background, hero, and enemy.